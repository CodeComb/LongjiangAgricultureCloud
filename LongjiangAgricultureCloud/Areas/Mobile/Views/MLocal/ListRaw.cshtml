@model IEnumerable<Information>
@{
    Layout = null;
}
@foreach (var i in Model)
{
    <a href="/Mobile/MLocal/Show/@i.ID">
        <div class="list-outer">
            @i.Title<br /><div class="sub-title">类型：@i.SupplyDemand / 价格：@i.Price</div>
        </div>
    </a>
}