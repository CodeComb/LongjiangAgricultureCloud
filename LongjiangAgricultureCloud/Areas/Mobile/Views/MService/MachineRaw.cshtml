@model IEnumerable<Information>
@{
    Layout = null;
}
@foreach (var i in Model)
{
    <a href="/Mobile/MService/ShowMachine/@i.ID">
        <div class="list-outer">
            @i.Title<br /><div class="sub-title">发布时间：@i.Time.ToString("yyyy-MM-dd HH:mm")</div>
        </div>
    </a>
}